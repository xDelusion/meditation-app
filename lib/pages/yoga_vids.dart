import 'package:flutter/material.dart';

class YogaVideos extends StatelessWidget {
  const YogaVideos({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Yoga Videos'),
      ),
    );
  }
}
